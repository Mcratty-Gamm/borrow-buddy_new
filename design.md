# Borrow Buddy: เอกสารออกแบบ (เวอร์ชัน 2)

ศัพท์ทั้งหมดในเอกสารนี้อ้างอิงจาก [CONTEXT.md](./CONTEXT.md)

**สิ่งที่เปลี่ยนจากเวอร์ชัน 1**
- ย้ายที่เก็บ Loan จาก localStorage ไป Supabase (Postgres)
- เพิ่มการเข้าสู่ระบบด้วยอีเมล + รหัสผ่านสำหรับเจ้าของ ปิดการสมัครสมาชิกเอง
- ใช้ RLS ให้แต่ละบัญชีเจ้าของเห็นและแก้ได้เฉพาะ Loan ของตัวเอง
- นำเข้าข้อมูลเดิมจาก localStorage ได้ครั้งเดียว
- ยังไม่มีการลบ Loan เหมือนเดิม

## 1. ภาพรวมและเป้าหมาย
เว็บหน้าเดียวสำหรับ **เจ้าของ** ใช้บันทึกว่า **เพื่อน** ยืม **ของ** อะไร เมื่อไร ต้องคืนเมื่อไร และกดว่า **คืนแล้ว** ได้ ข้อมูลเก็บบน Supabase จึงเปิดจากเครื่องไหนก็ได้หลัง **เข้าสู่ระบบ**

## 2. ขอบเขต

**อยู่ในขอบเขต**
- เข้าสู่ระบบด้วยอีเมล + รหัสผ่าน, ออกจากระบบ, จำเซสชันไว้หลังรีเฟรช
- เพิ่ม Loan, แก้ไข Loan, ดูรายการ Loan
- กดคืนแล้ว (ระบุวันที่คืนจริง) และยกเลิกการคืน
- ค้นหา/กรองตามชื่อเพื่อน
- แสดงสถานะ และจำนวนวันเกินกำหนด
- นำเข้าข้อมูลเดิมจาก localStorage (ครั้งเดียว)

**นอกขอบเขต (เวอร์ชัน 2)**
- สมัครสมาชิกเองจากหน้าเว็บ (บัญชีเจ้าของสร้างโดยผู้ดูแลใน Supabase Dashboard เท่านั้น)
- ลืมรหัสผ่าน/รีเซ็ตรหัสผ่านจากหน้าเว็บ, ล็อกอินผ่าน social/magic link
- เพื่อนเข้าเว็บ, แชร์ Loan ระหว่างบัญชี
- ลบ Loan, แจ้งเตือน, เลื่อนกำหนดคืน, รูปภาพของ
- ทะเบียนรายชื่อเพื่อน/ของแยก, การตรวจของซ้ำซ้อน
- ใช้งานออฟไลน์, อัปเดตแบบเรียลไทม์ข้ามเครื่อง, ส่งออกไฟล์

## 3. เทคโนโลยี
- React + Vite, ภาษา JavaScript (ไม่ใช้ TypeScript)
- Supabase: Postgres เก็บ Loan, Supabase Auth (อีเมล + รหัสผ่าน), RLS
- ไลบรารี `@supabase/supabase-js` ฝั่งเว็บ
- ทดสอบด้วย Vitest
- ค่าที่ใช้เชื่อมต่อเก็บใน `.env.local` (ไม่ commit): `VITE_SUPABASE_URL`, `VITE_SUPABASE_PUBLISHABLE_KEY`
- ห้ามใช้ service_role / secret key ในโค้ดฝั่งเว็บเด็ดขาด

## 4. โมเดลข้อมูล

**Loan (ฝั่งแอป)** ฟิลด์เหมือนเวอร์ชัน 1 ทุกตัว (ไม่มี owner เพราะ UI ไม่ต้องใช้)
| ฟิลด์ | ชนิด | หมายเหตุ |
|---|---|---|
| `id` | string (UUID) | สร้างโดยฐานข้อมูล (ยกเว้นตอนนำเข้าข้อมูลเดิม ใช้ id เดิม) |
| `friendName` | string | ชื่อเล่นของเพื่อน พิมพ์เองทุกครั้ง ห้ามว่าง |
| `itemName` | string | ชื่อของ ห้ามว่าง |
| `borrowedDate` | string `YYYY-MM-DD` | วันที่ยืม |
| `dueDate` | string `YYYY-MM-DD` | กำหนดคืน บังคับกรอก |
| `returnedDate` | string `YYYY-MM-DD` หรือ `null` | วันที่คืนจริง ว่างจนกว่าจะกดคืน |

**ตาราง `public.loans` (ฝั่ง Supabase)**
| คอลัมน์ | ชนิด | หมายเหตุ |
|---|---|---|
| `id` | `uuid` PK | default `gen_random_uuid()` |
| `owner_id` | `uuid` not null | FK → `auth.users(id)`, default `auth.uid()` |
| `friend_name` | `text` not null | check: ตัดช่องว่างแล้วไม่ว่าง |
| `item_name` | `text` not null | check: ตัดช่องว่างแล้วไม่ว่าง |
| `borrowed_date` | `date` not null | |
| `due_date` | `date` not null | check: `due_date >= borrowed_date` |
| `returned_date` | `date` null | check: `returned_date is null or returned_date >= borrowed_date` |
| `created_at` | `timestamptz` not null | default `now()` |

- index บน `owner_id` (ใช้กับ RLS)
- แปลงชื่อคอลัมน์ snake_case ↔ camelCase ในโค้ดฝั่งเว็บที่เดียว (`lib/loanMapper.js`)
- วันที่เก็บเป็น `date` (ค.ศ.) รับส่งเป็นสตริง `YYYY-MM-DD` และแสดงผลเป็น พ.ศ. เท่านั้น (เช่น 24 ก.ย. 2569)
- สถานะและจำนวนวันเกินกำหนดไม่เก็บ คำนวณจากวันที่ทุกครั้งเหมือนเดิม

## 5. กติกา

**สถานะ** (คำนวณจากวันนี้ตามเวลาเครื่องของเจ้าของ)
- **คืนแล้ว**: `returnedDate` ไม่เป็น null
- **เกินกำหนด**: ยังไม่คืน และวันนี้ > `dueDate`
- **ยังไม่คืน**: ยังไม่คืน และวันนี้ ≤ `dueDate` (ถึงกำหนดวันนี้พอดียังไม่เกิน)

**จำนวนวันเกินกำหนด** = วันนี้ − `dueDate` (นับเป็นวัน ใช้กับ Loan ที่เกินกำหนดเท่านั้น)

**การตรวจวันที่** (ปฏิเสธการบันทึกเมื่อผิดกติกา พร้อมข้อความภาษาไทย)
- `dueDate` ต้องไม่ก่อน `borrowedDate`
- `returnedDate` ต้องไม่ก่อน `borrowedDate`
- ชื่อเพื่อนและชื่อของห้ามว่าง
- ตรวจสองชั้น: ในแอปด้วย `validateLoan` ก่อนส่ง และ check constraint ในฐานข้อมูลเป็นด่านสุดท้าย

**การกดคืนแล้ว**
- ค่าเริ่มต้นของวันที่คืนจริงคือวันนี้ แก้เป็นวันย้อนหลังได้
- ยกเลิกการคืนได้ (ตั้ง `returnedDate` กลับเป็น null)

**การลบ**: ไม่มี ทั้งใน UI และฐานข้อมูล (ไม่มี RLS policy สำหรับ DELETE และเพิกถอนสิทธิ์ DELETE จาก role `anon`/`authenticated`)

## 6. การเข้าสู่ระบบและความปลอดภัย

**Supabase Auth**
- เปิดเฉพาะผู้ให้บริการ Email (อีเมล + รหัสผ่าน)
- ปิด "Allow new users to sign up" ในการตั้งค่า Auth ผู้ดูแลสร้างบัญชีเจ้าของใน Dashboard (Authentication → Users → Add user, ติ๊ก auto confirm)
- เว็บไม่มีฟอร์มสมัครสมาชิกและไม่เรียก `signUp`
- เซสชันจัดการโดย supabase-js (จำไว้ในเบราว์เซอร์, ต่ออายุโทเค็นอัตโนมัติ)

**RLS บนตาราง `loans`** (เปิด RLS, policy ทั้งหมดใช้กับ role `authenticated` เท่านั้น)
| การกระทำ | เงื่อนไข |
|---|---|
| SELECT | `owner_id = (select auth.uid())` |
| INSERT | with check `owner_id = (select auth.uid())` |
| UPDATE | using และ with check `owner_id = (select auth.uid())` (ย้ายเจ้าของไม่ได้) |
| DELETE | ไม่มี policy (ปฏิเสธเสมอ) |

- role `anon` (ยังไม่เข้าสู่ระบบ) อ่าน/เขียนอะไรไม่ได้เลย
- หลังสร้างตารางให้ตรวจ Security Advisor ของ Supabase ว่าไม่มีคำเตือน

## 7. หน้าจอ (หน้าเดียว)

**ยังไม่เข้าสู่ระบบ → หน้าเข้าสู่ระบบ**
- ช่องอีเมล, รหัสผ่าน, ปุ่ม "เข้าสู่ระบบ"
- ผิดพลาดแสดงข้อความภาษาไทยกลาง ๆ "อีเมลหรือรหัสผ่านไม่ถูกต้อง" (ไม่บอกว่าผิดช่องไหน) และแยกกรณีเชื่อมต่อไม่ได้
- ปุ่มปิดระหว่างรอผล ไม่มีลิงก์สมัครสมาชิก

**เข้าสู่ระบบแล้ว → หน้าหลัก** (เหมือนเวอร์ชัน 1 และเพิ่ม)
- แถบบนแสดงอีเมลเจ้าของ + ปุ่ม **ออกจากระบบ** + ปุ่มสลับโหมดมืด
- **ฟอร์ม Loan** ด้านบน ใช้ทั้งเพิ่มและแก้ไข: เพื่อน, ของ, วันที่ยืม (ค่าเริ่มต้นวันนี้), กำหนดคืน กดแก้ไขที่รายการแล้วข้อมูลขึ้นฟอร์ม มีปุ่มยกเลิกการแก้ไข
- **ช่องค้นหา/กรอง** ตามชื่อเพื่อน
- **รายการ Loan** แยกกลุ่มตามลำดับ **เกินกำหนด → ยังไม่คืน → คืนแล้ว** แต่ละกลุ่มเรียงตามกำหนดคืนใกล้สุดก่อน
- แต่ละรายการแสดง: ชื่อของ, ชื่อเพื่อน, วันที่ยืม, กำหนดคืน, สถานะ, จำนวนวันเกินกำหนด (เฉพาะเกินกำหนด), วันที่คืนจริง (เฉพาะคืนแล้ว)
- ปุ่มต่อรายการ: **คืนแล้ว**, **ยกเลิกการคืน**, **แก้ไข** ไม่มีปุ่มลบ
- สถานะกำลังโหลด และข้อความภาษาไทยเมื่อโหลด/บันทึกไม่สำเร็จ
- **แถบนำเข้าข้อมูลเดิม** แสดงเฉพาะเมื่อเข้าเงื่อนไขในข้อ 9

**การแสดงผล**
- ภาษาไทยทั้งหมด, วันที่ พ.ศ.
- รองรับหน้าจอมือถือ (layout ยืดหยุ่น)
- โหมดมืด สลับได้ด้วยปุ่ม และจำค่าที่เลือกไว้ (ยังเก็บใน localStorage เพราะเป็นค่าของเครื่อง ไม่ใช่ข้อมูล Loan)
- ใช้สี/ข้อความแยกสถานะให้ชัด ไม่พึ่งสีอย่างเดียว

## 8. การเก็บข้อมูล (Supabase)
- โหลด Loan ทั้งหมดของเจ้าของตอนเข้าสู่ระบบสำเร็จ/เปิดหน้าพร้อมเซสชันเดิม (RLS กรองให้เอง ไม่ต้องใส่เงื่อนไข owner ในคิวรี)
- เพิ่ม = `insert` หนึ่งแถว (ไม่ส่ง `id`/`owner_id` ให้ฐานข้อมูลสร้าง), แก้ไข/คืนแล้ว/ยกเลิกคืน = `update` เฉพาะแถวนั้นตาม `id`
- อัปเดต state ในหน้าเว็บ **หลัง** เซิร์ฟเวอร์ตอบสำเร็จ ใช้แถวที่เซิร์ฟเวอร์ส่งกลับ (ไม่ทำ optimistic update)
- บันทึกไม่สำเร็จ (เน็ตหลุด, ถูก constraint ปฏิเสธ, เซสชันหมดอายุ) ให้คงข้อมูลในฟอร์ม/รายการเดิมไว้และแจ้งเป็นภาษาไทย เซสชันหมดอายุให้กลับไปหน้าเข้าสู่ระบบ
- ออกจากระบบแล้วล้าง Loan ออกจาก state ทันที

## 9. การนำเข้าข้อมูลเดิม (ครั้งเดียว)
- **เงื่อนไขแสดงแถบ**: เข้าสู่ระบบแล้ว, localStorage คีย์ `borrow-buddy:loans` อ่านได้และมีอย่างน้อย 1 Loan, และยังไม่มีธง `borrow-buddy:imported` ในเครื่องนี้
- แถบบอกจำนวน Loan ที่พบ มีปุ่ม **นำเข้า** และ **ไม่นำเข้า**
- นำเข้า: ตรวจทุกรายการด้วย `validateLoan` ก่อน รายการที่ผิดกติกาไม่ส่งและแจ้งจำนวน รายการที่ผ่านส่งเป็น batch เดียวด้วย upsert แบบ ignore duplicates ใช้ `id` เดิม (ถ้า id เดิมไม่ใช่ UUID ให้สร้างใหม่) จึงกดซ้ำหรือนำเข้าจากหลายแท็บก็ไม่เกิดแถวซ้ำ
- สำเร็จ: ตั้งธง `borrow-buddy:imported`, โหลดรายการจาก Supabase ใหม่, แจ้งจำนวนที่นำเข้า
- ไม่นำเข้า: ตั้งธงเช่นกัน แถบไม่ขึ้นอีก
- **ไม่ลบข้อมูลเดิมใน localStorage** ทั้งกรณีสำเร็จและล้มเหลว (เก็บไว้เป็นสำรอง)
- JSON เสีย: ไม่แสดงแถบ และไม่เขียนทับข้อมูลเดิม

## 10. โครงสร้างโค้ด (ระดับโฟลเดอร์)

แยกตรรกะล้วนออกจาก UI เพื่อให้ทดสอบง่าย ฟังก์ชันที่คุยกับ Supabase รับ client เป็นพารามิเตอร์เพื่อให้ทดสอบด้วย client จำลองได้

```
borrow-buddy/
├── CONTEXT.md
├── design.md
├── .env.example             # ชื่อตัวแปร VITE_SUPABASE_* (ไม่มีค่าจริง)
├── supabase/
│   └── migrations/          # SQL สร้างตาราง loans, constraint, RLS, สิทธิ์
└── src/
    ├── main.jsx
    ├── App.jsx              # เลือกหน้า เข้าสู่ระบบ/หน้าหลัก ตามเซสชัน
    ├── components/
    │   ├── LoginForm.jsx
    │   ├── AccountBar.jsx   # อีเมล + ออกจากระบบ
    │   ├── ImportBanner.jsx
    │   ├── LoanForm.jsx
    │   ├── LoanList.jsx
    │   ├── LoanItem.jsx
    │   ├── SearchBox.jsx
    │   └── ThemeToggle.jsx
    └── lib/
        ├── supabaseClient.js # สร้าง client จาก env
        ├── auth.js           # เข้า/ออกจากระบบ, ติดตามเซสชัน, แปลงข้อผิดพลาดเป็นภาษาไทย
        ├── loanMapper.js     # แปลงแถว DB ↔ Loan
        ├── loanRepo.js       # โหลด/เพิ่ม/แก้ไข Loan ผ่าน Supabase
        ├── legacyImport.js   # อ่าน localStorage เดิม, เตรียมรายการนำเข้า, ธงนำเข้าแล้ว
        ├── storage.js        # (เดิม) อ่าน localStorage ใช้โดย legacyImport เท่านั้น
        ├── loanRules.js      # (เดิม)
        ├── dateFormat.js     # (เดิม)
        └── theme.js          # (เดิม)
```

ฟังก์ชันใน `lib/` รับ "วันนี้" เป็นพารามิเตอร์ ไม่เรียกเวลาจริงข้างใน เพื่อให้ทดสอบได้แน่นอน

## 11. การทดสอบ

**Vitest (ตรรกะใน `lib/`)**
- ของเดิมทั้งหมด: สถานะ, ขอบวันกำหนดคืน, จำนวนวันเกิน, ตรวจวันที่, จัดกลุ่ม/เรียง, แปลง พ.ศ., กรองชื่อเพื่อน
- `loanMapper`: แปลงไป-กลับครบทุกฟิลด์, `returnedDate` null
- `loanRepo` ด้วย client จำลอง: ส่งคอลัมน์ถูก, ไม่ส่ง `owner_id`, คืนข้อความไทยเมื่อ error
- `auth`: แปลงข้อผิดพลาดเป็นข้อความไทย
- `legacyImport`: เงื่อนไขแสดงแถบ, คัดรายการผิดกติกา, id ไม่ใช่ UUID, ธงนำเข้าแล้ว, ไม่แตะข้อมูลเดิม

**ตรวจฐานข้อมูลด้วยมือ (SQL / สองบัญชีทดสอบ)**
- บัญชี A มองไม่เห็นและแก้ Loan ของบัญชี B ไม่ได้
- ไม่เข้าสู่ระบบ อ่าน/เขียนไม่ได้
- DELETE ถูกปฏิเสธทุกกรณี
- ใส่ `owner_id` ของคนอื่นตอน insert/update ถูกปฏิเสธ
- check constraint ปฏิเสธวันที่/ชื่อผิดกติกา
- เรียก signUp ตรง ๆ ผ่าน API ถูกปฏิเสธ

## 12. ประเด็นที่ตั้งใจไม่ทำ
สมัครสมาชิกเอง, รีเซ็ตรหัสผ่านจากหน้าเว็บ, ลบ Loan, แจ้งเตือน, เลื่อนกำหนด, รูปภาพ, ทะเบียนเพื่อน/ของ, ออฟไลน์, เรียลไทม์, ส่งออกไฟล์ หากต้องการในอนาคตให้ทบทวน CONTEXT.md ก่อน
